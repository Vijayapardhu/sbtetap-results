services:
  - type: web
    name: sbtetap-results
    env: node
    buildCommand: |
      npm install
      npx playwright install --with-deps
    startCommand: npm start
    plan: free
    envVars:
      - key: NODE_ENV
        value: production
